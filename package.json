{
  "name": "ai-usage-badges",
  "version": "0.1.0",
  "private": true,
  "description": "Standardized badges for disclosing AI involvement in content creation.",
  "scripts": {
    "generate:badges": "python scripts/generate_badges.py",
    "export:png": "node scripts/export_png.js",
    "check:node": "node --check scripts/export_png.js"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
